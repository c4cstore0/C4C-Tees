<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>C4C TEES | Premium T-Shirts</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Montserrat:wght@700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --black: #000000;
            --volt: #ccff00;
            --volt-dark: #b3e600;
            --light-gray: #f5f5f5;
            --medium-gray: #e0e0e0;
            --dark-gray: #333333;
            --white: #ffffff;
            --transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            color: var(--black);
            background-color: var(--white);
            line-height: 1.6;
            position: relative;
            overflow-x: hidden;
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 800;
            letter-spacing: -0.5px;
        }
        
        .container {
            width: 90%;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 15px;
        }
        
        /* Header Styles */
        header {
            background-color: var(--white);
            position: sticky;
            top: 0;
            z-index: 100;
            padding: 20px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo-container {
            height: 40px;
            display: flex;
            align-items: center;
        }
        
        .logo-placeholder {
            background: var(--light-gray);
            border: 1px dashed var(--medium-gray);
            border-radius: 8px;
            width: 120px;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--dark-gray);
            font-size: 14px;
            font-weight: 600;
        }
        
        .header-actions {
            display: flex;
            gap: 20px;
            align-items: center;
        }
        
        .search-container {
            position: relative;
            display: flex;
            align-items: center;
        }
        
        .search-container input {
            padding: 10px 20px 10px 40px;
            border: 1px solid var(--medium-gray);
            border-radius: 30px;
            width: 220px;
            font-size: 14px;
            transition: var(--transition);
        }
        
        .search-container input:focus {
            outline: none;
            border-color: var(--volt);
            box-shadow: 0 0 0 3px rgba(204, 255, 0, 0.2);
            width: 250px;
        }
        
        .search-icon {
            position: absolute;
            left: 15px;
            color: var(--dark-gray);
        }
        
        .cart-icon {
            position: relative;
            cursor: pointer;
        }
        
        .cart-count {
            position: absolute;
            top: -8px;
            right: -8px;
            background: var(--volt);
            color: var(--black);
            width: 20px;
            height: 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: 700;
        }
        
        /* Cover Photo Slideshow */
        .cover-slideshow {
            height: 500px;
            position: relative;
            overflow: hidden;
            margin-bottom: 40px;
        }
        
        .slide {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 1s ease-in-out;
            background-size: cover;
            background-position: center;
        }
        
        .slide.active {
            opacity: 1;
        }
        
        .slide-content {
            position: absolute;
            bottom: 60px;
            left: 60px;
            max-width: 500px;
            color: var(--white);
            text-shadow: 0 2px 10px rgba(0,0,0,0.5);
        }
        
        .slide-content h2 {
            font-size: 48px;
            margin-bottom: 15px;
        }
        
        .slide-content p {
            font-size: 18px;
            margin-bottom: 25px;
            opacity: 0.9;
        }
        
        .slide-nav {
            position: absolute;
            bottom: 30px;
            right: 30px;
            display: flex;
            gap: 15px;
        }
        
        .slide-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: rgba(255,255,255,0.5);
            cursor: pointer;
            transition: var(--transition);
        }
        
        .slide-dot.active {
            background: var(--white);
            transform: scale(1.2);
        }
        
        /* Products Section */
        .section-title {
            text-align: center;
            margin: 60px 0 40px;
        }
        
        .section-title h2 {
            font-size: 42px;
            margin-bottom: 16px;
            position: relative;
            display: inline-block;
        }
        
        .section-title h2:after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: var(--volt);
            border-radius: 2px;
        }
        
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 40px;
            margin-bottom: 80px;
        }
        
        .product-card {
            background: var(--white);
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            transition: var(--transition);
            position: relative;
        }
        
        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }
        
        .product-gallery {
            height: 300px;
            position: relative;
            overflow: hidden;
        }
        
        .product-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 0.5s ease-in-out;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--light-gray);
        }
        
        .product-image.active {
            opacity: 1;
        }
        
        .product-image img {
            max-width: 85%;
            max-height: 85%;
            transition: var(--transition);
        }
        
        .gallery-nav {
            position: absolute;
            bottom: 15px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 8px;
        }
        
        .gallery-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: rgba(0,0,0,0.3);
            cursor: pointer;
            transition: var(--transition);
        }
        
        .gallery-dot.active {
            background: var(--black);
            transform: scale(1.3);
        }
        
        .product-info {
            padding: 24px;
        }
        
        .product-category {
            color: var(--volt);
            font-size: 14px;
            font-weight: 700;
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .product-title {
            font-size: 20px;
            margin-bottom: 15px;
            font-weight: 700;
        }
        
        /* Color Selection */
        .color-options {
            margin: 15px 0;
        }
        
        .color-options label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            font-size: 15px;
        }
        
        .colors {
            display: flex;
            gap: 10px;
        }
        
        .color-option {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            cursor: pointer;
            border: 2px solid transparent;
            transition: var(--transition);
        }
        
        .color-option:hover, .color-option.active {
            transform: scale(1.2);
            border-color: var(--black);
        }
        
        /* Material Selection */
        .material-options {
            margin: 15px 0;
        }
        
        .material-options label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            font-size: 15px;
        }
        
        .materials {
            display: flex;
            gap: 15px;
        }
        
        .material-option {
            padding: 10px 15px;
            border: 1px solid var(--medium-gray);
            border-radius: 8px;
            cursor: pointer;
            transition: var(--transition);
            font-weight: 600;
        }
        
        .material-option:hover, .material-option.active {
            border-color: var(--volt);
            background: var(--light-gray);
        }
        
        .product-price {
            font-size: 22px;
            font-weight: 800;
            margin: 15px 0;
            color: var(--black);
        }
        
        .product-actions {
            display: flex;
            gap: 12px;
            margin-top: 20px;
        }
        
        .add-to-cart {
            flex: 1;
            padding: 12px;
            background: var(--black);
            color: var(--white);
            border: none;
            border-radius: 8px;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }
        
        .add-to-cart:hover {
            background: var(--volt);
            color: var(--black);
        }
        
        .order-now {
            flex: 1;
            padding: 12px;
            background: #25D366;
            color: var(--white);
            border: none;
            border-radius: 8px;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }
        
        .order-now:hover {
            background: #128c7e;
        }
        
        /* Cart Sidebar */
        #cart-sidebar {
            position: fixed;
            top: 0;
            right: -400px;
            width: 400px;
            height: 100vh;
            background: var(--white);
            box-shadow: -5px 0 15px rgba(0,0,0,0.1);
            z-index: 1000;
            transition: var(--transition);
            padding: 20px;
            display: flex;
            flex-direction: column;
        }
        
        #cart-sidebar.open {
            right: 0;
        }
        
        .cart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--medium-gray);
        }
        
        .close-cart {
            background: none;
            border: none;
            font-size: 24px;
            cursor: pointer;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: var(--transition);
        }
        
        .close-cart:hover {
            background: var(--light-gray);
        }
        
        .cart-items {
            flex: 1;
            overflow-y: auto;
            padding-right: 10px;
        }
        
        .cart-item {
            display: flex;
            gap: 15px;
            padding: 15px 0;
            border-bottom: 1px solid var(--medium-gray);
        }
        
        .cart-item img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 8px;
        }
        
        .cart-item-details {
            flex: 1;
        }
        
        .cart-item-title {
            font-weight: 700;
            margin-bottom: 5px;
        }
        
        .cart-item-attributes {
            font-size: 14px;
            color: var(--dark-gray);
            margin-bottom: 8px;
        }
        
        .cart-item-price {
            font-weight: 700;
        }
        
        .remove-item {
            background: none;
            border: none;
            color: #e63946;
            cursor: pointer;
            font-size: 18px;
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .cart-footer {
            padding-top: 20px;
            border-top: 1px solid var(--medium-gray);
        }
        
        .cart-total {
            display: flex;
            justify-content: space-between;
            font-size: 20px;
            font-weight: 800;
            margin-bottom: 20px;
        }
        
        .checkout-btn {
            width: 100%;
            padding: 14px;
            background: var(--black);
            color: var(--white);
            border: none;
            border-radius: 8px;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .checkout-btn:hover {
            background: var(--volt);
            color: var(--black);
        }
        
        /* Overlay for cart */
        #cart-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 999;
            display: none;
        }
        
        #cart-overlay.active {
            display: block;
        }
        
        /* Contact & Payment Sections */
        .contact-section {
            padding: 80px 0;
            background: var(--black);
            color: var(--white);
        }
        
        .contact-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 30px;
            max-width: 1000px;
            margin: 0 auto;
        }
        
        .contact-item {
            display: flex;
            align-items: center;
            gap: 16px;
            padding: 20px 25px;
            border-radius: 12px;
            background: rgba(255,255,255,0.05);
            transition: var(--transition);
            min-width: 240px;
            border: 1px solid rgba(255,255,255,0.1);
        }
        
        .contact-item:hover {
            background: rgba(204,255,0,0.1);
            border-color: var(--volt);
        }
        
        .contact-icon {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            flex-shrink: 0;
            background: var(--volt);
            color: var(--black);
        }
        
        .contact-text h4 {
            font-size: 16px;
            margin-bottom: 4px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .contact-text p {
            font-size: 18px;
            font-weight: 700;
            margin: 0;
        }
        
        /* Payment Methods */
        .payment-section {
            padding: 60px 0;
            background: var(--light-gray);
            text-align: center;
        }
        
        .payment-methods {
            display: flex;
            justify-content: center;
            gap: 40px;
            flex-wrap: wrap;
            margin-top: 30px;
        }
        
        .payment-method {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 12px;
            padding: 25px;
            background: var(--white);
            border-radius: 16px;
            min-width: 200px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.05);
        }
        
        .payment-icon {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            color: var(--white);
        }
        
        .orange-money {
            background: #FFA500;
        }
        
        .mtn-momo {
            background: #009F48;
        }
        
        .payment-method h4 {
            font-size: 20px;
            margin-bottom: 8px;
        }
        
        .payment-method p {
            font-size: 24px;
            font-weight: 800;
            color: var(--black);
        }
        
        /* Footer */
        footer {
            background: var(--black);
            color: var(--white);
            padding: 70px 0 40px;
        }
        
        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }
        
        .footer-col h3 {
            font-size: 20px;
            margin-bottom: 24px;
            position: relative;
            padding-bottom: 10px;
        }
        
        .footer-col h3:after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 40px;
            height: 3px;
            background: var(--volt);
        }
        
        .footer-col ul {
            list-style: none;
        }
        
        .footer-col ul li {
            margin-bottom: 12px;
        }
        
        .footer-col ul li a {
            color: #aaa;
            text-decoration: none;
            transition: var(--transition);
            font-size: 16px;
        }
        
        .footer-col ul li a:hover {
            color: var(--volt);
        }
        
        .copyright {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid #333;
            color: #aaa;
            font-size: 15px;
        }
        
        /* Admin Button */
        #admin-button {
            position: fixed;
            bottom: 20px;
            left: 20px;
            width: 50px;
            height: 50px;
            background: rgba(0,0,0,0.01);
            border: none;
            cursor: pointer;
            z-index: 1000;
            opacity: 0.01;
            transition: opacity 0.3s;
        }
        
        #admin-button:hover {
            opacity: 0.3;
            background: rgba(0,0,0,0.1);
        }
        
        /* Admin Panel */
        #admin-panel {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.9);
            z-index: 2000;
            display: none;
            justify-content: center;
            align-items: center;
        }
        
        .admin-container {
            background: var(--white);
            width: 90%;
            max-width: 1000px;
            max-height: 90vh;
            border-radius: 16px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            box-shadow: 0 20px 50px rgba(0,0,0,0.3);
        }
        
        .admin-header {
            background: var(--black);
            color: var(--white);
            padding: 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .admin-header h2 {
            color: var(--white);
            font-size: 24px;
            font-weight: 800;
        }
        
        .close-admin {
            background: none;
            border: none;
            color: var(--white);
            font-size: 28px;
            cursor: pointer;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: var(--transition);
        }
        
        .close-admin:hover {
            background: var(--volt);
            color: var(--black);
        }
        
        .admin-content {
            display: flex;
            flex: 1;
            overflow: hidden;
        }
        
        .admin-sidebar {
            width: 250px;
            background: var(--light-gray);
            padding: 24px 0;
        }
        
        .admin-sidebar ul {
            list-style: none;
        }
        
        .admin-sidebar li {
            margin-bottom: 4px;
        }
        
        .admin-sidebar a {
            display: block;
            padding: 14px 24px;
            text-decoration: none;
            color: var(--black);
            font-weight: 600;
            transition: var(--transition);
            position: relative;
        }
        
        .admin-sidebar a:hover, .admin-sidebar a.active {
            background: var(--volt);
            color: var(--black);
        }
        
        .admin-main {
            flex: 1;
            padding: 24px;
            overflow-y: auto;
        }
        
        .admin-section {
            display: none;
        }
        
        .admin-section.active {
            display: block;
        }
        
        .form-group {
            margin-bottom: 24px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 10px;
            font-weight: 700;
            color: var(--black);
        }
        
        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 14px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-family: 'Inter', sans-serif;
            font-size: 16px;
            transition: var(--transition);
        }
        
        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--volt);
            box-shadow: 0 0 0 3px rgba(204, 255, 0, 0.2);
        }
        
        .form-group textarea {
            min-height: 120px;
            resize: vertical;
        }
        
        .btn-admin {
            background: var(--black);
            color: var(--white);
            border: none;
            padding: 14px 28px;
            border-radius: 8px;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }
        
        .btn-admin:hover {
            background: var(--volt);
            color: var(--black);
        }
        
        .btn-danger {
            background: #e63946;
        }
        
        .btn-danger:hover {
            background: #c1121f;
        }
        
        .product-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 24px;
            margin-top: 24px;
        }
        
        .admin-product-card {
            border: 1px solid #eee;
            border-radius: 12px;
            padding: 20px;
            background: #fafafa;
            transition: var(--transition);
        }
        
        .admin-product-card:hover {
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .admin-product-card img {
            width: 100%;
            height: 160px;
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: 16px;
        }
        
        .category-list {
            margin-top: 24px;
        }
        
        .category-item {
            display: flex;
            justify-content: space-between;
            padding: 16px;
            border-bottom: 1px solid #eee;
            transition: var(--transition);
        }
        
        .category-item:hover {
            background: var(--light-gray);
        }
        
        /* Password Modal */
        #password-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.85);
            z-index: 2100;
            display: none;
            justify-content: center;
            align-items: center;
        }
        
        .password-container {
            background: var(--white);
            padding: 40px;
            border-radius: 16px;
            width: 90%;
            max-width: 450px;
            text-align: center;
            box-shadow: 0 20px 50px rgba(0,0,0,0.3);
        }
        
        .password-container h3 {
            margin-bottom: 20px;
            font-size: 28px;
            font-weight: 800;
        }
        
        .password-container p {
            color: var(--dark-gray);
            margin-bottom: 24px;
            font-size: 18px;
        }
        
        .password-container input {
            width: 100%;
            padding: 16px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 18px;
            transition: var(--transition);
        }
        
        .password-container input:focus {
            outline: none;
            border-color: var(--volt);
            box-shadow: 0 0 0 3px rgba(204, 255, 0, 0.2);
        }
        
        .password-actions {
            margin-top: 20px;
            display: flex;
            gap: 12px;
            justify-content: center;
        }
        
        #password-error {
            color: #e63946;
            margin-top: 15px;
            font-weight: 600;
            display: none;
        }
        
        /* Responsive Design */
        @media (max-width: 992px) {
            .cover-slideshow {
                height: 400px;
            }
            
            .slide-content h2 {
                font-size: 36px;
            }
        }
        
        @media (max-width: 768px) {
            header {
                padding: 15px 0;
            }
            
            .search-container input {
                width: 180px;
                padding: 8px 16px 8px 36px;
                font-size: 13px;
            }
            
            .search-container input:focus {
                width: 200px;
            }
            
            .search-icon {
                left: 12px;
                font-size: 14px;
            }
            
            .cover-slideshow {
                height: 350px;
            }
            
            .slide-content {
                bottom: 40px;
                left: 30px;
                max-width: 90%;
            }
            
            .slide-content h2 {
                font-size: 32px;
            }
            
            .slide-content p {
                font-size: 16px;
            }
            
            .products-grid {
                grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
                gap: 30px;
            }
            
            #cart-sidebar {
                width: 320px;
            }
        }
        
        @media (max-width: 480px) {
            .cover-slideshow {
                height: 300px;
            }
            
            .slide-content {
                bottom: 30px;
                left: 20px;
            }
            
            .slide-content h2 {
                font-size: 28px;
            }
            
            .products-grid {
                grid-template-columns: 1fr;
            }
            
            .product-actions {
                flex-direction: column;
            }
            
            .admin-content {
                flex-direction: column;
            }
            
            .admin-sidebar {
                width: 100%;
                display: flex;
                overflow-x: auto;
            }
            
            .admin-sidebar ul {
                display: flex;
                flex-wrap: nowrap;
            }
            
            .admin-sidebar li {
                margin: 0 5px;
            }
            
            .admin-sidebar a {
                white-space: nowrap;
                padding: 12px 16px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <!-- Invisible Admin Button -->
    <button id="admin-button" title="Admin Access"></button>

    <!-- Header -->
    <header>
        <div class="logo-container">
            <div class="logo-placeholder">Upload Logo</div>
        </div>
        <div class="header-actions">
            <div class="search-container">
                <i class="fas fa-search search-icon"></i>
                <input type="text" placeholder="Search products...">
            </div>
            <div class="cart-icon" id="open-cart">
                <i class="fas fa-shopping-bag"></i>
                <span class="cart-count">0</span>
            </div>
        </div>
    </header>

    <!-- Cover Photo Slideshow -->
    <div class="cover-slideshow">
        <div class="slide active" style="background-image: url('https://images.unsplash.com/photo-1521335629791-ce4aec67dd15?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1200&q=80');">
            <div class="slide-content">
                <h2>Premium Tees for Conscious Consumers</h2>
                <p>Discover our collection of ethically made, sustainably sourced t-shirts</p>
                <a href="#products" class="btn" style="background: var(--black); color: var(--white); display: inline-flex; padding: 12px 28px;">Shop Now</a>
            </div>
        </div>
        <div class="slide" style="background-image: url('https://images.unsplash.com/photo-1583744925-66324d957909?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1200&q=80');">
            <div class="slide-content">
                <h2>New Collection: Ocean Conservation</h2>
                <p>10% of proceeds support marine conservation efforts</p>
                <a href="#products" class="btn" style="background: var(--black); color: var(--white); display: inline-flex; padding: 12px 28px;">Explore</a>
            </div>
        </div>
        <div class="slide-nav">
            <div class="slide-dot active"></div>
            <div class="slide-dot"></div>
        </div>
    </div>

    <!-- Products Section -->
    <section class="products" id="products">
        <div class="container">
            <div class="section-title">
                <h2>Featured Collection</h2>
            </div>
            <div class="products-grid" id="products-container">
                <!-- Products will be loaded here dynamically -->
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact-section" id="contact">
        <div class="container">
            <div class="section-title">
                <h2 style="color: var(--white);">Contact Information</h2>
            </div>
            <div class="contact-container">
                <div class="contact-item">
                    <div class="contact-icon">
                        <i class="fab fa-whatsapp"></i>
                    </div>
                    <div class="contact-text">
                        <h4>WhatsApp</h4>
                        <p>+237 776839092</p>
                    </div>
                </div>
                
                <div class="contact-item">
                    <div class="contact-icon">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <div class="contact-text">
                        <h4>Email</h4>
                        <p>c4cstore0@gmail.com</p>
                    </div>
                </div>
                
                <div class="contact-item">
                    <div class="contact-icon">
                        <i class="fab fa-facebook-f"></i>
                    </div>
                    <div class="contact-text">
                        <h4>Facebook</h4>
                        <p>C4C TEES</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Payment Methods -->
    <section class="payment-section">
        <div class="container">
            <div class="section-title">
                <h2>Payment Methods</h2>
            </div>
            <div class="payment-methods">
                <div class="payment-method">
                    <div class="payment-icon orange-money">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <h4>Orange Money</h4>
                    <p>0776839092</p>
                </div>
                <div class="payment-method">
                    <div class="payment-icon mtn-momo">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <h4>MTN MoMo</h4>
                    <p>0880466699</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-grid">
                <div class="footer-col">
                    <h3>C4C TEES</h3>
                    <p>Premium t-shirts with purpose. Every purchase supports environmental and social causes.</p>
                </div>
                <div class="footer-col">
                    <h3>Shop</h3>
                    <ul>
                        <li><a href="#products">All Tees</a></li>
                        <li><a href="#products">New Arrivals</a></li>
                        <li><a href="#products">Best Sellers</a></li>
                        <li><a href="#products">Sale</a></li>
                    </ul>
                </div>
                <div class="footer-col">
                    <h3>Company</h3>
                    <ul>
                        <li><a href="#">Our Story</a></li>
                        <li><a href="#">Sustainability</a></li>
                        <li><a href="#">Impact Report</a></li>
                    </ul>
                </div>
                <div class="footer-col">
                    <h3>Support</h3>
                    <ul>
                        <li><a href="#contact">Contact Us</a></li>
                        <li><a href="#">FAQs</a></li>
                        <li><a href="#">Shipping Info</a></li>
                        <li><a href="#">Returns & Exchanges</a></li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                <p>&copy; 2023 C4C TEES. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Cart Sidebar -->
    <div id="cart-sidebar">
        <div class="cart-header">
            <h3>Your Cart</h3>
            <button class="close-cart" id="close-cart">&times;</button>
        </div>
        <div class="cart-items" id="cart-items">
            <!-- Cart items will be added here -->
        </div>
        <div class="cart-footer">
            <div class="cart-total">
                <span>Total:</span>
                <span id="cart-total">$0.00</span>
            </div>
            <button class="checkout-btn" id="checkout-btn">Checkout via WhatsApp</button>
        </div>
    </div>
    
    <div id="cart-overlay"></div>

    <!-- Password Modal -->
    <div id="password-modal">
        <div class="password-container">
            <h3>Admin Access</h3>
            <p>Enter your admin password to access the control panel</p>
            <input type="password" id="password-input" placeholder="Admin password">
            <div id="password-error">Incorrect password. Please try again.</div>
            <div class="password-actions">
                <button class="btn-admin" id="submit-password">Submit</button>
                <button class="btn-admin btn-danger" id="cancel-password">Cancel</button>
            </div>
        </div>
    </div>

    <!-- Admin Panel -->
    <div id="admin-panel">
        <div class="admin-container">
            <div class="admin-header">
                <h2>C4C TEES Admin Panel</h2>
                <button class="close-admin">&times;</button>
            </div>
            <div class="admin-content">
                <div class="admin-sidebar">
                    <ul>
                        <li><a href="#" class="admin-tab active" data-tab="dashboard">Dashboard</a></li>
                        <li><a href="#" class="admin-tab" data-tab="products">Products</a></li>
                        <li><a href="#" class="admin-tab" data-tab="categories">Categories</a></li>
                        <li><a href="#" class="admin-tab" data-tab="slideshow">Slideshow</a></li>
                        <li><a href="#" class="admin-tab" data-tab="settings">Settings</a></li>
                    </ul>
                </div>
                <div class="admin-main">
                    <!-- Dashboard Section -->
                    <div class="admin-section active" id="dashboard">
                        <h3>Admin Dashboard</h3>
                        <p>Welcome to your C4C TEES admin panel. From here you can manage all aspects of your store.</p>
                        <div style="margin-top: 30px; display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 24px;">
                            <div style="background: var(--light-gray); padding: 24px; border-radius: 12px;">
                                <h4>Products</h4>
                                <p id="product-count" style="font-size: 28px; font-weight: 800; margin-top: 10px;">0</p>
                            </div>
                            <div style="background: var(--light-gray); padding: 24px; border-radius: 12px;">
                                <h4>Categories</h4>
                                <p id="category-count" style="font-size: 28px; font-weight: 800; margin-top: 10px;">0</p>
                            </div>
                            <div style="background: var(--light-gray); padding: 24px; border-radius: 12px;">
                                <h4>Orders</h4>
                                <p style="font-size: 28px; font-weight: 800; margin-top: 10px;">12</p>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Products Section -->
                    <div class="admin-section" id="products">
                        <h3>Manage Products</h3>
                        <button class="btn-admin" id="add-new-product"><i class="fas fa-plus"></i> Add New Product</button>
                        
                        <div id="product-form" style="display: none; margin-top: 24px; padding: 24px; background: var(--light-gray); border-radius: 12px;">
                            <h4 id="form-title">Add New Product</h4>
                            <div class="form-group">
                                <label for="product-name">Product Name</label>
                                <input type="text" id="product-name" placeholder="Enter product name">
                            </div>
                            <div class="form-group">
                                <label for="product-category">Category</label>
                                <select id="product-category">
                                    <option value="">Select a category</option>
                                    <!-- Categories will be populated dynamically -->
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="product-price-cotton">Price (Cotton)</label>
                                <input type="number" id="product-price-cotton" placeholder="Enter cotton price" step="0.01" min="0">
                            </div>
                            <div class="form-group">
                                <label for="product-price-polyester">Price (Polyester)</label>
                                <input type="number" id="product-price-polyester" placeholder="Enter polyester price" step="0.01" min="0">
                            </div>
                            <div class="form-group">
                                <label for="product-colors">Available Colors (comma separated)</label>
                                <input type="text" id="product-colors" placeholder="e.g., Black, White, Navy">
                            </div>
                            <div class="form-group">
                                <label for="product-images">Image URLs (comma separated, 4 images)</label>
                                <textarea id="product-images" placeholder="Enter 4 image URLs separated by commas"></textarea>
                            </div>
                            <div class="form-group">
                                <label for="product-description">Description</label>
                                <textarea id="product-description" placeholder="Enter product description"></textarea>
                            </div>
                            <button class="btn-admin" id="save-product"><i class="fas fa-save"></i> Save Product</button>
                            <button class="btn-admin btn-danger" id="cancel-product" style="margin-left: 12px;"><i class="fas fa-times"></i> Cancel</button>
                        </div>
                        
                        <div class="product-list" id="admin-products-list">
                            <!-- Products will be loaded here -->
                        </div>
                    </div>
                    
                    <!-- Categories Section -->
                    <div class="admin-section" id="categories">
                        <h3>Manage Categories</h3>
                        <div class="form-group">
                            <label for="new-category">Add New Category</label>
                            <div style="display: flex; gap: 12px;">
                                <input type="text" id="new-category" placeholder="Enter new category name">
                                <button class="btn-admin" id="add-category" style="height: 52px;"><i class="fas fa-plus"></i> Add</button>
                            </div>
                        </div>
                        
                        <div class="category-list" id="categories-list">
                            <!-- Categories will be loaded here -->
                        </div>
                    </div>
                    
                    <!-- Slideshow Section -->
                    <div class="admin-section" id="slideshow">
                        <h3>Manage Slideshow</h3>
                        <p>Upload banner images for your homepage slideshow</p>
                        <div class="form-group">
                            <label for="slide1">Slide 1 Image URL</label>
                            <input type="text" id="slide1" placeholder="Enter image URL">
                        </div>
                        <div class="form-group">
                            <label for="slide2">Slide 2 Image URL</label>
                            <input type="text" id="slide2" placeholder="Enter image URL">
                        </div>
                        <div class="form-group">
                            <label for="slide3">Slide 3 Image URL</label>
                            <input type="text" id="slide3" placeholder="Enter image URL">
                        </div>
                        <button class="btn-admin"><i class="fas fa-save"></i> Update Slideshow</button>
                    </div>
                    
                    <!-- Settings Section -->
                    <div class="admin-section" id="settings">
                        <h3>Store Settings</h3>
                        <div class="form-group">
                            <label for="store-name">Store Name</label>
                            <input type="text" id="store-name" value="C4C TEES">
                        </div>
                        <div class="form-group">
                            <label for="admin-password">Admin Password</label>
                            <input type="password" id="admin-password" placeholder="Enter new password">
                        </div>
                        <div class="form-group">
                            <label for="logo-upload">Brand Logo</label>
                            <input type="text" id="logo-upload" placeholder="Enter logo image URL">
                            <div style="margin-top: 10px; text-align: center;">
                                <img id="logo-preview" src="" alt="Logo Preview" style="max-width: 150px; max-height: 50px; display: none;">
                            </div>
                        </div>
                        <button class="btn-admin"><i class="fas fa-save"></i> Save Settings</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Sample data - in a real app this would come from a database
        let products = [
            {
                id: 1,
                name: "Wave Rider Tee",
                category: "Ocean Conservation",
                prices: { cotton: 29.99, polyester: 24.99 },
                colors: ["Black", "White", "Navy"],
                images: [
                    "https://placehold.co/300x300/000000/ccff00?text=Front+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Back+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Worn+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Detail+View&font=montserrat"
                ],
                description: "Support ocean conservation with this premium tee featuring a wave design."
            },
            {
                id: 2,
                name: "Evergreen Guardian",
                category: "Forest Protection",
                prices: { cotton: 29.99, polyester: 24.99 },
                colors: ["Green", "Black", "Olive"],
                images: [
                    "https://placehold.co/300x300/000000/ccff00?text=Front+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Back+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Worn+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Detail+View&font=montserrat"
                ],
                description: "Protect our forests with this eco-friendly tee featuring evergreen trees."
            },
            {
                id: 3,
                name: "Safari Spirit",
                category: "Wildlife Conservation",
                prices: { cotton: 29.99, polyester: 24.99 },
                colors: ["Beige", "Brown", "Black"],
                images: [
                    "https://placehold.co/300x300/000000/ccff00?text=Front+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Back+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Worn+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Detail+View&font=montserrat"
                ],
                description: "Support wildlife conservation with this animal-inspired tee."
            },
            {
                id: 4,
                name: "Future Forward",
                category: "Climate Action",
                prices: { cotton: 29.99, polyester: 24.99 },
                colors: ["Gray", "Black", "White"],
                images: [
                    "https://placehold.co/300x300/000000/ccff00?text=Front+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Back+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Worn+View&font=montserrat",
                    "https://placehold.co/300x300/000000/ccff00?text=Detail+View&font=montserrat"
                ],
                description: "Take action for our climate with this forward-thinking tee design."
            }
        ];
        
        let categories = ["Ocean Conservation", "Forest Protection", "Wildlife Conservation", "Climate Action"];
        const ADMIN_PASSWORD = "admin123";
        let cart = [];
        
        // DOM Elements
        const adminButton = document.getElementById('admin-button');
        const passwordModal = document.getElementById('password-modal');
        const passwordInput = document.getElementById('password-input');
        const submitPasswordBtn = document.getElementById('submit-password');
        const cancelPasswordBtn = document.getElementById('cancel-password');
        const passwordError = document.getElementById('password-error');
        const adminPanel = document.getElementById('admin-panel');
        const closeAdminBtn = document.querySelector('.close-admin');
        const adminTabs = document.querySelectorAll('.admin-tab');
        const productsContainer = document.getElementById('products-container');
        const adminProductsList = document.getElementById('admin-products-list');
        const categoriesList = document.getElementById('categories-list');
        const addNewProductBtn = document.getElementById('add-new-product');
        const productForm = document.getElementById('product-form');
        const formTitle = document.getElementById('form-title');
        const saveProductBtn = document.getElementById('save-product');
        const cancelProductBtn = document.getElementById('cancel-product');
        const addCategoryBtn = document.getElementById('add-category');
        const newCategoryInput = document.getElementById('new-category');
        const productCategorySelect = document.getElementById('product-category');
        const productCount = document.getElementById('product-count');
        const categoryCount = document.getElementById('category-count');
        const openCartBtn = document.getElementById('open-cart');
        const closeCartBtn = document.getElementById('close-cart');
        const cartSidebar = document.getElementById('cart-sidebar');
        const cartOverlay = document.getElementById('cart-overlay');
        const cartItemsContainer = document.getElementById('cart-items');
        const cartTotalElement = document.getElementById('cart-total');
        const checkoutBtn = document.getElementById('checkout-btn');
        const logoPreview = document.getElementById('logo-preview');
        const logoUpload = document.getElementById('logo-upload');
        
        // Current product being edited
        let currentProduct = null;
        
        // Initialize the store
        function initStore() {
            renderProducts();
            updateAdminCounts();
            setupSlideshow();
            setupCartFunctionality();
        }
        
        // Render products on the main page
        function renderProducts() {
            productsContainer.innerHTML = '';
            products.forEach(product => {
                const productCard = document.createElement('div');
                productCard.className = 'product-card';
                productCard.innerHTML = `
                    <div class="product-gallery">
                        ${product.images.map((img, index) => 
                            `<div class="product-image ${index === 0 ? 'active' : ''}" data-index="${index}">
                                <img src="${img}" alt="${product.name} - View ${index + 1}">
                            </div>`
                        ).join('')}
                        <div class="gallery-nav">
                            ${product.images.map((_, index) => 
                                `<div class="gallery-dot ${index === 0 ? 'active' : ''}" data-index="${index}"></div>`
                            ).join('')}
                        </div>
                    </div>
                    <div class="product-info">
                        <div class="product-category">${product.category}</div>
                        <h3 class="product-title">${product.name}</h3>
                        
                        <div class="color-options">
                            <label>Color:</label>
                            <div class="colors">
                                ${product.colors.map(color => 
                                    `<div class="color-option" style="background-color: ${getColorCode(color)};" title="${color}"></div>`
                                ).join('')}
                            </div>
                        </div>
                        
                        <div class="material-options">
                            <label>Material:</label>
                            <div class="materials">
                                <div class="material-option active" data-material="cotton">Cotton - $${product.prices.cotton.toFixed(2)}</div>
                                <div class="material-option" data-material="polyester">Polyester - $${product.prices.polyester.toFixed(2)}</div>
                            </div>
                        </div>
                        
                        <div class="product-price">$${product.prices.cotton.toFixed(2)}</div>
                        
                        <div class="product-actions">
                            <button class="add-to-cart" data-id="${product.id}">
                                <i class="fas fa-shopping-cart"></i> Add to Cart
                            </button>
                            <a href="https://wa.me/237776839092?text=I'm%20interested%20in%20ordering%20${encodeURIComponent(product.name)}" class="order-now">
                                <i class="fab fa-whatsapp"></i> Order Now
                            </a>
                        </div>
                    </div>
                `;
                productsContainer.appendChild(productCard);
                
                // Add event listeners for gallery dots
                const dots = productCard.querySelectorAll('.gallery-dot');
                dots.forEach(dot => {
                    dot.addEventListener('click', (e) => {
                        const index = parseInt(e.target.dataset.index);
                        const gallery = e.target.closest('.product-gallery');
                        gallery.querySelectorAll('.product-image').forEach(img => img.classList.remove('active'));
                        gallery.querySelectorAll('.gallery-dot').forEach(d => d.classList.remove('active'));
                        gallery.querySelector(`.product-image[data-index="${index}"]`).classList.add('active');
                        e.target.classList.add('active');
                    });
                });
                
                // Add event listeners for material selection
                const materialOptions = productCard.querySelectorAll('.material-option');
                materialOptions.forEach(option => {
                    option.addEventListener('click', (e) => {
                        materialOptions.forEach(opt => opt.classList.remove('active'));
                        e.target.classList.add('active');
                        const material = e.target.dataset.material;
                        const priceElement = productCard.querySelector('.product-price');
                        priceElement.textContent = `$${product.prices[material].toFixed(2)}`;
                    });
                });
            });
        }
        
        // Helper function to get color codes
        function getColorCode(colorName) {
            const colorMap = {
                'Black': '#000000',
                'White': '#ffffff',
                'Navy': '#000080',
                'Green': '#008000',
                'Olive': '#808000',
                'Beige': '#f5f5dc',
                'Brown': '#a52a2a',
                'Gray': '#808080'
            };
            return colorMap[colorName] || '#cccccc';
        }
        
        // Update admin dashboard counts
        function updateAdminCounts() {
            productCount.textContent = products.length;
            categoryCount.textContent = categories.length;
        }
        
        // Render products in admin panel
        function renderAdminProducts() {
            adminProductsList.innerHTML = '';
            products.forEach(product => {
                const productCard = document.createElement('div');
                productCard.className = 'admin-product-card';
                productCard.innerHTML = `
                    <img src="${product.images[0]}" alt="${product.name}">
                    <h4>${product.name}</h4>
                    <p><strong>Category:</strong> ${product.category}</p>
                    <p><strong>Colors:</strong> ${product.colors.join(', ')}</p>
                    <p><strong>Cotton:</strong> $${product.prices.cotton.toFixed(2)} | <strong>Polyester:</strong> $${product.prices.polyester.toFixed(2)}</p>
                    <div style="margin-top: 16px; display: flex; gap: 10px;">
                        <button class="btn-admin edit-product" data-id="${product.id}"><i class="fas fa-edit"></i> Edit</button>
                        <button class="btn-admin btn-danger delete-product" data-id="${product.id}"><i class="fas fa-trash"></i> Delete</button>
                    </div>
                `;
                adminProductsList.appendChild(productCard);
            });
            
            // Add event listeners to edit/delete buttons
            document.querySelectorAll('.edit-product').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const id = parseInt(e.target.closest('button').dataset.id);
                    editProduct(id);
                });
            });
            
            document.querySelectorAll('.delete-product').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const id = parseInt(e.target.closest('button').dataset.id);
                    deleteProduct(id);
                });
            });
        }
        
        // Render categories in admin panel
        function renderCategories() {
            categoriesList.innerHTML = '';
            categories.forEach((category, index) => {
                const categoryItem = document.createElement('div');
                categoryItem.className = 'category-item';
                categoryItem.innerHTML = `
                    <span>${category}</span>
                    <button class="btn-admin btn-danger delete-category" data-index="${index}"><i class="fas fa-trash"></i></button>
                `;
                categoriesList.appendChild(categoryItem);
            });
            
            // Add event listeners to delete buttons
            document.querySelectorAll('.delete-category').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const index = parseInt(e.target.dataset.index);
                    deleteCategory(index);
                });
            });
            
            // Update category select in product form
            updateCategorySelect();
        }
        
        // Update category select dropdown
        function updateCategorySelect() {
            productCategorySelect.innerHTML = '<option value="">Select a category</option>';
            categories.forEach(category => {
                const option = document.createElement('option');
                option.value = category;
                option.textContent = category;
                productCategorySelect.appendChild(option);
            });
        }
        
        // Show password modal
        adminButton.addEventListener('click', () => {
            passwordModal.style.display = 'flex';
            passwordInput.value = '';
            passwordError.style.display = 'none';
        });
        
        // Handle password submission
        submitPasswordBtn.addEventListener('click', () => {
            if (passwordInput.value === ADMIN_PASSWORD) {
                passwordModal.style.display = 'none';
                adminPanel.style.display = 'flex';
                renderAdminProducts();
                renderCategories();
                updateAdminCounts();
            } else {
                passwordError.style.display = 'block';
            }
        });
        
        // Cancel password modal
        cancelPasswordBtn.addEventListener('click', () => {
            passwordModal.style.display = 'none';
        });
        
        // Close admin panel
        closeAdminBtn.addEventListener('click', () => {
            adminPanel.style.display = 'none';
        });
        
        // Tab navigation
        adminTabs.forEach(tab => {
            tab.addEventListener('click', (e) => {
                e.preventDefault();
                const target = e.target.dataset.tab;
                
                // Update active tab
                adminTabs.forEach(t => t.classList.remove('active'));
                e.target.classList.add('active');
                
                // Show corresponding section
                document.querySelectorAll('.admin-section').forEach(section => {
                    section.classList.remove('active');
                });
                document.getElementById(target).classList.add('active');
            });
        });
        
        // Add new product button
        addNewProductBtn.addEventListener('click', () => {
            currentProduct = null;
            formTitle.textContent = 'Add New Product';
            document.getElementById('product-name').value = '';
            document.getElementById('product-category').value = '';
            document.getElementById('product-price-cotton').value = '';
            document.getElementById('product-price-polyester').value = '';
            document.getElementById('product-colors').value = '';
            document.getElementById('product-images').value = '';
            document.getElementById('product-description').value = '';
            productForm.style.display = 'block';
        });
        
        // Cancel product form
        cancelProductBtn.addEventListener('click', () => {
            productForm.style.display = 'none';
        });
        
        // Save product
        saveProductBtn.addEventListener('click', () => {
            const name = document.getElementById('product-name').value;
            const category = document.getElementById('product-category').value;
            const cottonPrice = parseFloat(document.getElementById('product-price-cotton').value);
            const polyesterPrice = parseFloat(document.getElementById('product-price-polyester').value);
            const colors = document.getElementById('product-colors').value.split(',').map(c => c.trim()).filter(c => c);
            const images = document.getElementById('product-images').value.split(',').map(img => img.trim()).filter(img => img);
            const description = document.getElementById('product-description').value;
            
            if (!name || !category || isNaN(cottonPrice) || isNaN(polyesterPrice) || colors.length === 0 || images.length !== 4) {
                alert('Please fill in all required fields. You must provide exactly 4 image URLs.');
                return;
            }
            
            if (currentProduct) {
                // Update existing product
                const product = products.find(p => p.id === currentProduct.id);
                if (product) {
                    product.name = name;
                    product.category = category;
                    product.prices = { cotton: cottonPrice, polyester: polyesterPrice };
                    product.colors = colors;
                    product.images = images;
                    product.description = description;
                }
            } else {
                // Add new product
                const newProduct = {
                    id: products.length > 0 ? Math.max(...products.map(p => p.id)) + 1 : 1,
                    name,
                    category,
                    prices: { cotton: cottonPrice, polyester: polyesterPrice },
                    colors,
                    images,
                    description
                };
                products.push(newProduct);
            }
            
            renderProducts();
            renderAdminProducts();
            updateAdminCounts();
            productForm.style.display = 'none';
        });
        
        // Edit product
        function editProduct(id) {
            const product = products.find(p => p.id === id);
            if (product) {
                currentProduct = product;
                formTitle.textContent = 'Edit Product';
                document.getElementById('product-name').value = product.name;
                document.getElementById('product-category').value = product.category;
                document.getElementById('product-price-cotton').value = product.prices.cotton;
                document.getElementById('product-price-polyester').value = product.prices.polyester;
                document.getElementById('product-colors').value = product.colors.join(', ');
                document.getElementById('product-images').value = product.images.join(', ');
                document.getElementById('product-description').value = product.description;
                productForm.style.display = 'block';
            }
        }
        
        // Delete product
        function deleteProduct(id) {
            if (confirm('Are you sure you want to delete this product?')) {
                products = products.filter(p => p.id !== id);
                renderProducts();
                renderAdminProducts();
                updateAdminCounts();
            }
        }
        
        // Add category
        addCategoryBtn.addEventListener('click', () => {
            const categoryName = newCategoryInput.value.trim();
            if (categoryName && !categories.includes(categoryName)) {
                categories.push(categoryName);
                newCategoryInput.value = '';
                renderCategories();
                updateAdminCounts();
            } else if (categories.includes(categoryName)) {
                alert('Category already exists!');
            } else {
                alert('Please enter a valid category name');
            }
        });
        
        // Delete category
        function deleteCategory(index) {
            const category = categories[index];
            const productsInCategory = products.filter(p => p.category === category);
            
            if (productsInCategory.length > 0) {
                if (!confirm(`There are ${productsInCategory.length} products in this category. Deleting it will remove these products from their category. Continue?`)) {
                    return;
                }
                
                // Remove category from products
                productsInCategory.forEach(product => {
                    product.category = '';
                });
            }
            
            categories.splice(index, 1);
            renderCategories();
            renderProducts();
            renderAdminProducts();
            updateAdminCounts();
        }
        
        // Slideshow functionality
        function setupSlideshow() {
            const slides = document.querySelectorAll('.slide');
            const dots = document.querySelectorAll('.slide-dot');
            let currentSlide = 0;
            
            function showSlide(index) {
                slides.forEach(slide => slide.classList.remove('active'));
                dots.forEach(dot => dot.classList.remove('active'));
                slides[index].classList.add('active');
                dots[index].classList.add('active');
                currentSlide = index;
            }
            
            dots.forEach((dot, index) => {
                dot.addEventListener('click', () => showSlide(index));
            });
            
            // Auto-rotate slides
            setInterval(() => {
                currentSlide = (currentSlide + 1) % slides.length;
                showSlide(currentSlide);
            }, 5000);
        }
        
        // Cart functionality
        function setupCartFunctionality() {
            // Open cart
            openCartBtn.addEventListener('click', () => {
                cartSidebar.classList.add('open');
                cartOverlay.classList.add('active');
            });
            
            // Close cart
            closeCartBtn.addEventListener('click', () => {
                cartSidebar.classList.remove('open');
                cartOverlay.classList.remove('active');
            });
            
            // Close cart when clicking overlay
            cartOverlay.addEventListener('click', () => {
                cartSidebar.classList.remove('open');
                cartOverlay.classList.remove('active');
            });
            
            // Add to cart buttons
            document.addEventListener('click', (e) => {
                if (e.target.classList.contains('add-to-cart') || e.target.closest('.add-to-cart')) {
                    const button = e.target.classList.contains('add-to-cart') ? e.target : e.target.closest('.add-to-cart');
                    const productId = parseInt(button.dataset.id);
                    const productCard = button.closest('.product-card');
                    
                    // Get selected material
                    const selectedMaterial = productCard.querySelector('.material-option.active').dataset.material;
                    
                    // Get selected color (first one for simplicity)
                    const selectedColor = productCard.querySelector('.color-option').title;
                    
                    addToCart(productId, selectedMaterial, selectedColor);
                }
            });
            
            // Checkout button
            checkoutBtn.addEventListener('click', () => {
                if (cart.length === 0) {
                    alert('Your cart is empty!');
                    return;
                }
                
                let message = 'Hello! I would like to order the following items:\n\n';
                cart.forEach(item => {
                    message += `- ${item.product.name} (${item.color}, ${item.material}) - $${item.price.toFixed(2)}\n`;
                });
                message += `\nTotal: $${calculateTotal().toFixed(2)}\n\nPlease confirm my order.`;
                
                window.open(`https://wa.me/237776839092?text=${encodeURIComponent(message)}`, '_blank');
            });
        }
        
        function addToCart(productId, material, color) {
            const product = products.find(p => p.id === productId);
            if (!product) return;
            
            const item = {
                id: Date.now(),
                productId,
                product,
                material,
                color,
                price: product.prices[material]
            };
            
            cart.push(item);
            updateCartDisplay();
            
            // Show feedback
            const button = document.querySelector(`.add-to-cart[data-id="${productId}"]`);
            const originalText = button.innerHTML;
            button.innerHTML = '<i class="fas fa-check"></i> Added!';
            setTimeout(() => {
                button.innerHTML = originalText;
            }, 1500);
        }
        
        function updateCartDisplay() {
            // Update cart count
            document.querySelector('.cart-count').textContent = cart.length;
            
            // Update cart items
            cartItemsContainer.innerHTML = '';
            if (cart.length === 0) {
                cartItemsContainer.innerHTML = '<p style="text-align: center; padding: 20px; color: var(--dark-gray);">Your cart is empty</p>';
            } else {
                cart.forEach(item => {
                    const cartItem = document.createElement('div');
                    cartItem.className = 'cart-item';
                    cartItem.innerHTML = `
                        <img src="${item.product.images[0]}" alt="${item.product.name}">
                        <div class="cart-item-details">
                            <div class="cart-item-title">${item.product.name}</div>
                            <div class="cart-item-attributes">${item.color} • ${item.material.charAt(0).toUpperCase() + item.material.slice(1)}</div>
                            <div class="cart-item-price">$${item.price.toFixed(2)}</div>
                        </div>
                        <button class="remove-item" data-id="${item.id}">&times;</button>
                    `;
                    cartItemsContainer.appendChild(cartItem);
                });
                
                // Add event listeners to remove buttons
                document.querySelectorAll('.remove-item').forEach(btn => {
                    btn.addEventListener('click', (e) => {
                        const id = parseInt(e.target.dataset.id);
                        cart = cart.filter(item => item.id !== id);
                        updateCartDisplay();
                    });
                });
            }
            
            // Update total
            cartTotalElement.textContent = `$${calculateTotal().toFixed(2)}`;
        }
        
        function calculateTotal() {
            return cart.reduce((total, item) => total + item.price, 0);
        }
        
        // Logo preview
        logoUpload.addEventListener('input', () => {
            if (logoUpload.value) {
                logoPreview.src = logoUpload.value;
                logoPreview.style.display = 'block';
            } else {
                logoPreview.style.display = 'none';
            }
        });
        
        // Initialize the store
        initStore();
    </script>
</body>
</html>